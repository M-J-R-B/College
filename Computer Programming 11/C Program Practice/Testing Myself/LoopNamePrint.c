#include<stdio.h>

int main () {

    char name, end;

    while (name=end) { 
            printf("Enter a name (Type END to exit): ");
            scanf("%c", &name);
            printf("%c", name);

    }
    
}